# Three-host Kashnio runtime verification

On 2026-09-05, the fixed three-validator core completed its Kashnio runtime-name
migration without changing validator keys, voting identities, finalized
membership, executable bytes, vote journals, or signed chain history.

All three validator services reported zero restart loops after their planned
cutovers. A common sample matched height 330,211, checkpoint identity, state
root, and the canonical three-member snapshot on all three hosts. Public
finality then advanced beyond height 330,229.

The public Explorer passed 14 automated tests, TypeScript validation, a
production build, and an isolated canary. Its live status reported all
components operational. The external surface watchdog also completed with zero
failures.

Retired runtime and build directories, disabled rehearsal tooling, obsolete
accounts, and unfinished backup staging data were removed. Previously verified
encrypted off-host recovery objects remain available.
